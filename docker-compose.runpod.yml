# Runpod deployment overrides for docker-compose.yml
services:
  video-merger:
    environment:
      - RUN_ENV=runpod
      - DATA_SHARED_BASE=/workspace/data/shared
      - REDIS_URL=redis://192.168.68.52:8061/2
      - COMFYUI_URL=https://q6r4k4drowe1hx-8188.proxy.runpod.net/api
      - ENABLE_IMAGE_GEN=true
      - ENABLE_VOICEOVER_GEN=false
      - COMFYUI_TIMEOUT_SECONDS=600
      - COMFYUI_POLL_INTERVAL_SECONDS=15
      - LOG_LEVEL=DEBUG
      - UVICORN_LOG_LEVEL=debug
    ports:
      - "8086:8000"
    volumes:
      - "/workspace/data/shared:/data/shared"

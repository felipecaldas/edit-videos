# Runpod deployment overrides for docker-compose.yml
services:
  video-merger:
    environment:
      - RUN_ENV=runpod
      - DATA_SHARED_BASE=/data/shared
      - REDIS_URL=redis://192.168.68.52:8061/2
      - COMFYUI_URL=https://dpb6nr54btmu2v-8188.proxy.runpod.net/api
      - ENABLE_IMAGE_GEN=true
      - ENABLE_VOICEOVER_GEN=false
      - COMFYUI_TIMEOUT_SECONDS=600
      - COMFYUI_POLL_INTERVAL_SECONDS=40
      - LOG_LEVEL=DEBUG
      - UVICORN_LOG_LEVEL=debug
      - WORKFLOW_IMAGE_PATH=/app/videomerge/comfyui-workflows/qwen-image-fast.Olivio.json
      - WORKFLOW_I2V_PATH=/app/videomerge/comfyui-workflows/I2V-Wan 2.2 Lightning.json
    ports:
      - "8086:8000"
    volumes:
      - "/mnt/tp-share/n8n-shared:/data/shared"
